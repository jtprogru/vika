[pytest]
python_files = "test*.py"
norecursedirs = ".git"
                ".tox"
                "venv*"
                "requirements*"
addopts = --verbose
          --strict-markers
          --color=yes
          --tb=short

testpaths = vika/tests

